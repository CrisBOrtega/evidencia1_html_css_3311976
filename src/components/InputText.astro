---
// Definimos las propiedades 
// que puede recibir el componente
    interface Props {
       etiqueta: string;
    }

    //crear una variable
    //para recibir la etiqueta
    //Astro.props=los valores desde
    //            el control         
    const  etiqueta  = Astro.props.etiqueta;

---

<!-- 
Etiqueta input:
se utiliza para los siguientes
controles:
- campo de texto
- password 
- checkbox
- radio button
- boton de envio
ES ETIQUETA CERRADA
-->
<label >{ etiqueta }:</label>
<input type="text" />
<style>
    /*solo se puede poner aqui */
    /* sintaxis css*/
    /*estilos para el input text*/
    /* selector de etiqueta: */
    /* estilos aplicables a toda etiqueta */
    /* señalada por el selector */
input[type="text"]{
        background-color: chartreuse;
        width: 400px;
        height: 20px;
        color: blue;
        /* bordes: 
        - color
        - peso (grosor)
        - estilo (tipo de linea)
        */
        border-color:brown;
        border-width: 3px;
        border-style: solid;
        border-radius: 5px;
    }
</style>